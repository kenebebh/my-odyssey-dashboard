import { IExperience } from "@/lib/types/experiences";
import React from "react";

export default function EditExperienceForm({
  experienceData,
}: {
  experienceData: IExperience;
}) {
  return <div>edit-experience-form</div>;
}
